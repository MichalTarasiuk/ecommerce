#import "../fragments/AccountError.graphql"

mutation ConfirmAccount($email: String!, $token: String!) {
  confirmAccount(email: $email, token: $token) {
    errors {
      ...AccountErrorFragment
    }
    user {
      email
      isActive
    }
  }
}
